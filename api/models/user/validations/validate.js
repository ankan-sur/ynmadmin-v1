/**
* Validation code for field keys on user
* @param {import("gadget-server").UserKeysFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.keys` to get the fields's data
  // add to the `errors` with `errors.add('keys', "did not pass the validation")`
};